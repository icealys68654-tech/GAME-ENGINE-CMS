{
  "name": "game-engine-cms",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, content-driven elemental world generation engine.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
